apiVersion: v1
kind: Service
metadata:
  name: {{ include "oracle-exporter.fullname" . }}
  labels:
    {{- include "oracle-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "oracle-exporter.selectorLabels" . | nindent 4 }}
