apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ibm-mq-consumer.fullname" . }}-secret
  labels:
    {{- include "ibm-mq-consumer.labels" . | nindent 4 }}
type: Opaque
data:
  mq-password: {{ .Values.mq.password | b64enc }}
