apiVersion: v1
kind: Service
metadata:
  name: {{ include "perf-tester.fullname" . }}
  labels:
    {{- include "perf-tester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "perf-tester.selectorLabels" . | nindent 4 }}
