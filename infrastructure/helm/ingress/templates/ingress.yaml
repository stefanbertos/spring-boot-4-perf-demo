{{- if .Values.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: perf-demo-ingress
  labels:
    app.kubernetes.io/name: perf-demo-ingress
    app.kubernetes.io/instance: {{ .Release.Name }}
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.className }}
  ingressClassName: {{ .Values.className }}
  {{- end }}
  rules:
    - host: {{ .Values.host }}
      http:
        paths:
          # Swagger UI and OpenAPI docs - route directly to perf-tester
          - path: /swagger-ui
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.services.swagger.name }}
                port:
                  number: {{ .Values.services.swagger.port }}
          - path: /v3/api-docs
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.services.swagger.name }}
                port:
                  number: {{ .Values.services.swagger.port }}
          # Route all other traffic through API Gateway
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.services.gateway.name }}
                port:
                  number: {{ .Values.services.gateway.port }}
  {{- if .Values.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.host }}
      secretName: {{ .Values.tls.secretName | default "perf-demo-tls" }}
  {{- end }}
{{- end }}
